# If you can compile please compile it to windows executable
- this demo contains the binary for linux, and also, the source code. And .sh file that can be run to produce a binary.
- please make sure to have
SDL2 and SDL2_image, if you don't have them please install using (Debian)
'''
sudo apt update
sudo apt install libsdl2-dev libsdl2-image-dev
'''

also if you don't have gcc compiler please install using (Debian)
'''
sudo apt install build-essential
'''
